# --- Main requirements file --- #
# This file aggregates all dependencies for the project, including both backend and testing requirements.
# It allows developers to install all necessary packages with a single command.

-r src/security_shield/security-backend/requirements.txt
-r tests/requirements.txt

# Additional development dependencies for code formatting and linting
setuptools>=61.0
wheel
black==24.1.1
flake8==7.0.0
requests-mock==1.12.1